<?php
require_once __DIR__ . '/backend/bootstrap.php';
require_role(['vendeur','admin']);
$sales = get_sales_by_seller(current_user_id());
$pageTitle = 'Mes ventes';
require_once __DIR__ . '/partials/header.php';
?>
<section class="page-title"><h1>Mes ventes</h1><p>Produits vendus et acheteurs associés.</p></section>
<div class="table-wrap"><table><thead><tr><th>Date</th><th>Produit</th><th>Acheteur</th><th>Quantité</th><th>Prix</th></tr></thead><tbody>
<?php foreach ($sales as $s): ?><tr><td><?= e($s['date_commande']) ?></td><td><?= e($s['titre']) ?></td><td><?= e($s['prenom'] . ' ' . $s['nom']) ?></td><td><?= (int)$s['quantite'] ?></td><td><?= money_format_fr((float)$s['prix_unitaire']) ?></td></tr><?php endforeach; ?>
</tbody></table></div>
<?php require_once __DIR__ . '/partials/footer.php'; ?>
