<?php
require_once __DIR__ . '/backend/bootstrap.php';
require_login();
$order = get_order_details((int)($_GET['id'] ?? 0), current_user_id());
if (!$order) { http_response_code(404); exit('Commande introuvable.'); }
$pageTitle = 'Confirmation - Mercato Nova';
require_once __DIR__ . '/partials/header.php';
?>
<section class="success-panel">
    <p class="eyebrow">Transaction validée</p><h1>Commande confirmée</h1>
    <p>Commande n°<?= (int)$order['id_order'] ?> — statut : <strong><?= e($order['statut_commande']) ?></strong></p>
    <p>Paiement : <?= e($order['payment']['methode'] ?? 'simulé') ?> — <?= e($order['payment']['statut_paiement'] ?? '') ?></p>
</section>
<div class="cart-list">
    <?php foreach ($order['items'] as $item): ?><article class="cart-item"><img src="<?= e(url($item['image'] ?: DEFAULT_PRODUCT_IMAGE)) ?>" alt="<?= e($item['titre']) ?>"><div><h3><?= e($item['titre']) ?></h3><p><?= (int)$item['quantite'] ?> × <?= money_format_fr((float)$item['prix_unitaire']) ?></p></div></article><?php endforeach; ?>
</div>
<div class="actions-row" style="margin-top:20px"><a class="btn" href="<?= e(url('mes-achats.php')) ?>">Voir mes achats</a><a class="btn secondary" href="<?= e(url('catalogue.php')) ?>">Retour catalogue</a></div>
<?php require_once __DIR__ . '/partials/footer.php'; ?>
